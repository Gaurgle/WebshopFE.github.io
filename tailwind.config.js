module.exports = {
    darkMode: 'class', // or 'media' for media query based
    content: [
        "./src/**/*.{html,js}", // adjust based on your project structure
    ],
    theme: {
        extend: {},
    },
    plugins: [],
}